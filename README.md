Just need to add the file .env with the right credentials of client (id client and secret code mentionned in the report)
and install dependencies needed : npm install express body-parser express-session passport passport-local passport-google-oauth20 and launch the server with node controller.js and type in local browser : Https://localhost:3000
